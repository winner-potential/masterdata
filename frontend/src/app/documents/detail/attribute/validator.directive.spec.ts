import { AttributeValidatorDirective } from './validator.directive';

describe('AttributeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
